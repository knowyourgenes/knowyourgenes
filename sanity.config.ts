// The Sanity CLI looks for `sanity.config.ts` at the project root.
// The real configuration lives under `src/sanity/` with the rest of the app.
export { default } from './src/features/blog/sanity/config';
